name: Build and Push
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build Docker image
        run: docker build -t my-app:${{ github.sha }} .
      - name: Push to Docker Hub
        run: echo "Aquí irían los comandos para push a Docker Hub"
      - name: Deploy to Kubernetes
        run: echo "Aquí irían los comandos para desplegar en Kubernetes"