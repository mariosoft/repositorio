name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Print a greeting
        run: echo "¡Hola Mundo! Este es mi primer pipeline."
